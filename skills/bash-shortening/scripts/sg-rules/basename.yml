id: bash-shorten-basename
language: bash
severity: hint
message: 'basename of a quoted variable collapses to ${X##*/} — same result, no subprocess.'
rule:
  all:
    - pattern: '$(basename "$ARG")'
    - has:
        kind: variable_name
        pattern: $V
        stopBy: end
fix: '${$V##*/}'
